import { registerRootComponent } from 'expo';
import App from './App'; // Assure-toi que c'est bien './App' et pas autre chose

console.log('index.js - App component:', App);

registerRootComponent(App);
